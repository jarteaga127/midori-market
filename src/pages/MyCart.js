import React from "react";
import styled from "styled-components";

const MyCart001 = styled.div`
margin-top: 62px;
`;

export default function MyCart() {
    return(
<MyCart001>Your cart is empty. Time to start filling it up.</MyCart001>
    )
}